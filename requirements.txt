streamlit>=1.36
pandas>=2.0
requests>=2.31

```
保存文件（Ctrl + S）

### 3. **确认文件结构**

现在你的文件夹应该是：
```
aura_demo/
├── app.py
├── requirements.txt  ← 新创建的
├── pages/
├── agents_stub/
└── data/
```

### 4. **在终端安装和运行**

确保终端在aura_demo文件夹（看到`PS C:\Users\HOPE\Desktop\aura_demo>`）

依次运行：
```
pip install streamlit pandas requests
```
等待安装完成，然后：
```
streamlit run app.py